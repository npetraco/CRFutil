model
{

  # Likelihood
  for (i in 1:N) {
    #y[i] ~ dbin(p[i], 1)
    #logit(p[i]) <- inprod(x[i,],theta[])
    y[i] ~ dpois(lambda[i])
    log(lambda[i]) <- alpha + inprod(Mmodl[i,],theta[])
  }

  # Priors
  alpha ~ dnorm(0,0.01)
  for(i in 1:p) {
    theta[i] ~ dnorm(0,0.01)
  }

}
