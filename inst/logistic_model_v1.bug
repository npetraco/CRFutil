model
{
  # Likelihood
  for (t in 1:TT) {
    y[t] ~ dbin(p[t], K)
    logit(p[t]) <- alpha + inprod(x[t,],beta[])
  }
  # Priors
  alpha ~ dnorm(0.0,0.01)
  for(j in 1:pp) {
      beta[j]~dnorm(0,0.01)
  }
}
